Improvements ideas:
1) Save all direction in database to have statistic and offer some trip to user
2) Need Fix: current searching nearest events is performance hit so we need to optimize it
and as well we just searching by radius
3) Currently we search by First and Last points, we need to search by all existing points
but this feature will be implemented after release