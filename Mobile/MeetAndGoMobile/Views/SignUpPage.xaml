<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MeetAndGoMobile.Views.SignUpPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behaviours="clr-namespace:MeetAndGoMobile.Infrastructure.Behaviours;assembly=MeetAndGoMobile.Infrastructure"
    xmlns:constants="clr-namespace:MeetAndGoMobile.Infrastructure.Constants;assembly=MeetAndGoMobile.Infrastructure"
    xmlns:effects="clr-namespace:MeetAndGoMobile.Infrastructure.Effects;assembly=MeetAndGoMobile.Infrastructure"
    xmlns:extensions="clr-namespace:MeetAndGoMobile.Infrastructure.Extensions;assembly=MeetAndGoMobile.Infrastructure"
    BackgroundColor="{x:Static constants:Colors.BgrBrown}"
    ControlTemplate="{StaticResource BaseContentTemplate}"
    NavigationPage.HasNavigationBar="False">

    <ContentPage.Resources>
        <ResourceDictionary>


            <x:Double x:Key="ContentWidth">250</x:Double>

            <!--  Flex Layout style  -->

            <Style TargetType="FlexLayout">
                <Setter Property="AlignContent" Value="Start" />
                <Setter Property="AlignItems" Value="Center" />
                <Setter Property="Direction" Value="Column" />
            </Style>

            <!--  Label Logo style  -->

            <Style x:Key="LabelLogoStyle" TargetType="Label">
                <Setter Property="FontSize" Value="{x:Static constants:FontSize.XxxLarge}" />
                <Setter Property="Text" Value="{extensions:Translation U_MeetAndGo}" />
                <Setter Property="TextColor" Value="White" />
            </Style>

            <!--  Button frame style  -->

            <Style x:Key="ButtonFrameGreenStyle" TargetType="Frame">
                <Setter Property="BorderColor" Value="{x:Static constants:Colors.Green}" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="HasShadow" Value="False" />
                <Setter Property="WidthRequest" Value="{StaticResource ContentWidth}" />
                <Setter Property="Padding" Value="15" />
            </Style>

            <!--  Button Label style  -->

            <Style x:Key="ButtonLabelGreenStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{x:Static constants:Colors.Green}" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="FontSize" Value="{x:Static constants:FontSize.ExtraLarge}" />
            </Style>

            <!--  Entry Label Info style  -->

            <Style x:Key="EntryLabelInfoStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{x:Static constants:Colors.Gray}" />
                <Setter Property="FontSize" Value="{x:Static constants:FontSize.Large}" />
                <Setter Property="WidthRequest" Value="{StaticResource ContentWidth}" />
                <Setter Property="HorizontalTextAlignment" Value="Start" />
            </Style>

            <!--  Entry style  -->

            <Style x:Key="EntryStyle" TargetType="Entry">
                <Setter Property="TextColor" Value="White" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="WidthRequest" Value="{StaticResource ContentWidth}" />
            </Style>

        </ResourceDictionary>
    </ContentPage.Resources>

    <FlexLayout>

        <Image Source="mg_pin.png" />

        <Label Style="{StaticResource LabelLogoStyle}" />

        <Label Style="{StaticResource EntryLabelInfoStyle}" Text="{extensions:Translation EntryYourPhoneNumber}" />

        <Entry
            Margin="0"
            Style="{StaticResource EntryStyle}"
            Text="{Binding PhoneNumberText}">
            <Entry.Effects>
                <effects:SimplifyEntryEffect />
            </Entry.Effects>
        </Entry>

        <Frame Style="{StaticResource ButtonFrameGreenStyle}">

            <Frame.Behaviors>
                <behaviours:FrameHorizontalRoundingBehavior />
            </Frame.Behaviors>

            <Frame.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ContinueCommand}" />
            </Frame.GestureRecognizers>

            <Label Style="{StaticResource ButtonLabelGreenStyle}" Text="{extensions:Translation Continue}" />

        </Frame>

        <!--  Terms  -->

        <Label FontSize="{x:Static constants:FontSize.Medium}" HorizontalTextAlignment="Center">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="{extensions:Translation BySigningYourAgree}" TextColor="{x:Static constants:Colors.DarkGray}" />
                    <Span Text="&#x0a;" />
                    <Span Text="{extensions:Translation TermsAndPolicy}" TextColor="{x:Static constants:Colors.Gray}" />
                </FormattedString>
            </Label.FormattedText>

            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding TermCommand}" />
            </Label.GestureRecognizers>

        </Label>

        <!--  Sign In  -->

        <Label FontSize="{x:Static constants:FontSize.Large}" HorizontalTextAlignment="Center">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="{extensions:Translation AlreadyHaveAccount}" TextColor="{x:Static constants:Colors.DarkGray}" />
                    <Span Text="&#x0a;" />
                    <Span Text="{extensions:Translation SignInCommand}" TextColor="{x:Static constants:Colors.Green}" />
                </FormattedString>
            </Label.FormattedText>

            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding SignInCommand}" />
            </Label.GestureRecognizers>

        </Label>

    </FlexLayout>

</ContentPage>